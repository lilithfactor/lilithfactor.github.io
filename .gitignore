# Secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build output & generated types
dist/
.vite/
.astro/

# The 2019 site, kept as a memento until launch — never committed
dump/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Source audio (multi-MB mp3s). Transcoded opus/m4a in public/audio/ is what ships.
bgm/

# Hi-fi model downloads of unknown provenance. Superseded by poly.pizza assets,
# which carry their licence. Kept locally, never shipped.
models-src/

# Resume & portfolio working files — CONFIDENTIAL, never committed.
# This repo is public. These hold absolute OnArrival revenue, partner names,
# take rates, internal reports and the unredacted source PDFs. The published
# artefacts are the Notion case studies, which are redacted on purpose.
resume/
portfolio/
